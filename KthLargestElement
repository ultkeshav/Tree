class Solution {
public:
    int count = 0;
    int result = -1;

    int kthSmallest(TreeNode* root, int k) {
        inorder(root, k);
        return result;
    }

    void inorder(TreeNode* root, int k) {
        if (!root) return;
        
        inorder(root->left, k);
        
        count++;
        if (count == k) {
            result = root->val;
            return;
        }
        
        inorder(root->right, k);
    }
};
✅ Time Complexity:
